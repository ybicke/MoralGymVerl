# =============================================================================
# SDPO run 1 — gemma-2-9b-it. The shared recipe lives in _base_sdpo_pd.yaml;
# this config only pins the model explicitly (path and chat template
# otherwise come implicitly from _base_clariden.yaml — restated here so the
# run's model is visible in the config name and content).
#
# Run-1 question for gemma: does D_A D_O prose move off 6% during training,
# i.e. does SDPO elicit what prompting could not (probe B says the signal
# exists: +1.74 -> +3.02)?
#
# Submit:
#   DATASET_CONFIG=_base_sdpo_pd sbatch scripts/slurm/train_verl.sh \
#       gemma2_9b_sdpo_pd_deon-repair-gen_tft_70 <run_name>
# =============================================================================
defaults:
  - _base_sdpo_pd
  - _self_

actor_rollout_ref:
  model:
    path: google/gemma-2-9b-it
