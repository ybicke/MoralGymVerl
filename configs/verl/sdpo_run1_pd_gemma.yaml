# =============================================================================
# SDPO run 1 — gemma-2-9b-it. The shared recipe lives in sdpo_run1_pd.yaml;
# this config only pins the model explicitly (path and chat template
# otherwise come implicitly from moralgym_user.yaml — restated here so the
# run's model is visible in the config name and content).
#
# Run-1 question for gemma: does D_A D_O prose move off 6% during training,
# i.e. does SDPO elicit what prompting could not (probe B says the signal
# exists: +1.74 -> +3.02)?
#
# Submit:
#   DATASET_CONFIG=sdpo_run1_pd sbatch scripts/slurm/train_verl.sh \
#       sdpo_run1_pd_gemma <run_name>
# =============================================================================
defaults:
  - sdpo_run1_pd
  - _self_

actor_rollout_ref:
  model:
    path: google/gemma-2-9b-it
